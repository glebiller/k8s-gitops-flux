---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: prod
  namespace: flux-cluster
spec:
  interval: 24h
  path: ./prod/
  prune: true
  sourceRef:
    kind: GitRepository
    name: k8s-gitops-flux-cluster
  validation: client
